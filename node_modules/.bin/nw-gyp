#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nw-gyp/bin/nw-gyp.js" "$@"
  ret=$?
else 
  node  "$basedir/../nw-gyp/bin/nw-gyp.js" "$@"
  ret=$?
fi
exit $ret
